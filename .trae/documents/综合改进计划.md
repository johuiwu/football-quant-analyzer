# 足球竞彩量化分析系统 - 综合改进计划

> **计划日期**: 2026-05-31
> **当前状态**: AppNew.tsx 已拆分到 1802 行 (降幅 26%)，但 quantModel.ts 仍高达 3222 行，是最大瓶颈

---

## 现状总览

| 文件 | 行数 | 状态 | 优先级 |
|------|------|------|--------|
| `src/utils/quantModel.ts` | **3222** | 核心瓶颈，亟需拆分 | 🔴 P0 |
| `src/AppNew.tsx` | **1802** | 已降 26%，仍需拆分 | 🟠 P1 |
| `src/App.tsx` | **2427** | 原始文件，待替换 | 🟠 P1 |
| `src/services/ValidationService.ts` | **60** | 独立，结构良好 | 🟢 OK |
| `src/utils/oddsCalculator.ts` | ~100 | 独立，结构良好 | 🟢 OK |

---

## 第一阶段：quantModel.ts 拆分 (P0 — 投入产出比最高)

### 问题

`quantModel.ts` 的 `calculateBetsModel` 单函数超过 2000 行，是所有问题的根源：无法测试、无法维护、修改风险极高。

### 拆分方案

将 `quantModel.ts` 拆分为 **6 个独立模块**：

```
src/utils/
├── quantModel.ts            # 重导出入口（向后兼容）
├── quant/
│   ├── index.ts             # 统一导出
│   ├── poisson.ts           # Poisson 分布 & Dixon-Coles 调整
│   ├── elo.ts               # Elo 评级系统
│   ├── kelly.ts             # 凯利公式
│   ├── features.ts          # 特征工程 & 扩展特征提取
│   ├── channels/
│   │   ├── oddsChannel.ts   # 欧赔通道计算
│   │   └── asianChannel.ts  # 亚盘通道计算
│   ├── fusion.ts            # 通道融合 & 综合决策
│   ├── bayesian.ts          # 贝叶斯实时更新
│   ├── asianHandicap.ts     # 亚盘/欧赔互转 & 特征提取
│   └── types.ts             # 集中类型定义
```

### 每个模块的构建步骤

#### Task 1: 提取类型定义 → `quant/types.ts`

**目标**: 将所有接口和类型从 `quantModel.ts` 集中到 `quant/types.ts`

**涉及类型**:
```typescript
// 从 quantModel.ts 原始文件提取
export interface PredictionResults { ... }
export interface ModelWeights { ... }
export interface AdvancedParams { ... }
export interface LiveBayesianParams { ... }
export interface BetsModelInput { ... }
export interface AsianHandicapParams { ... }
export interface AsianHandicapFeatures { ... }
export interface ExtendedTeamFeatures { ... }
```

**操作**:
- 创建 `src/utils/quant/types.ts`
- 从 `quantModel.ts` 剪切类型定义
- 更新 `quantModel.ts` 的 import 从 `./quant/types` 导入
- 验证: `npm run dev` 启动无编译错误

---

#### Task 2: 提取 Poisson 模块 → `quant/poisson.ts`

**目标**: 提取 `poisson()` 和 `dixonColesAdjustment()` 两个函数

**同时修复数值稳定性问题**:

```typescript
// quant/poisson.ts
export function poisson(k: number, lambda: number): number {
  if (lambda <= 0) return k === 0 ? 1 : 0;
  if (k > 170) return 0; // 防止溢出
  const logP = -lambda + k * Math.log(lambda) - logFactorial(k);
  return Math.exp(logP);
}

function logFactorial(n: number): number {
  if (n <= 1) return 0;
  let result = 0;
  for (let i = 2; i <= n; i++) {
    result += Math.log(i);
  }
  return result;
}

export function dixonColesAdjustment(
  x: number, y: number,
  lambda: number, mu: number,
  rho: number = -0.075
): number {
  if (x === 0 && y === 0) return 1 - lambda * mu * rho;
  if (x === 1 && y === 0) return 1 + mu * rho;
  if (x === 0 && y === 1) return 1 + lambda * rho;
  if (x === 1 && y === 1) return 1 - rho;
  return 1.0;
}
```

**操作**:
- 创建 `src/utils/quant/poisson.ts`
- 从 `quantModel.ts` 剪切两个函数
- 在 `quantModel.ts` 中从 `./quant/poisson` 重新导入
- **关键**: 改为对数阶乘防止大数溢出
- 验证: `npm run dev` 启动无错误

---

#### Task 3: 提取 Elo 模块 → `quant/elo.ts`

**目标**: 提取 `getTeamElo()` 和 `LEAGUE_ELO_BASE` 常量，并增加动态更新能力

```typescript
// quant/elo.ts
const LEAGUE_ELO_BASE: Record<string, number> = { /* 原样迁移 */ };

const ELO_MAP: Record<string, number> = { /* 原样迁移 */ };

export function getTeamElo(team: TeamStats): number {
  const teamRank = team?.rank ?? 10;
  const league = team?.league || 'DEFAULT';
  return ELO_MAP[team?.id] || (LEAGUE_ELO_BASE[league] + (10 - teamRank) * 12);
}

/** 新增: 动态 Elo 更新 */
export function updateElo(
  currentElo: number,
  opponentElo: number,
  result: 'win' | 'draw' | 'loss',
  kFactor: number = 32
): number {
  const expected = 1 / (1 + Math.pow(10, (opponentElo - currentElo) / 400));
  const actual = result === 'win' ? 1 : result === 'draw' ? 0.5 : 0;
  return Math.round(currentElo + kFactor * (actual - expected));
}
```

**操作**:
- 创建 `src/utils/quant/elo.ts`
- 剪切 `getTeamElo()` 和 `LEAGUE_ELO_BASE`
- 新增 `updateElo()` 函数
- 验证: `npm run dev`

---

#### Task 4: 提取凯利模块 → `quant/kelly.ts`

**目标**: 提取 `calculateKellyFraction()`

```typescript
// quant/kelly.ts
export function calculateKellyFraction(prob: number, decimalOdds: number): number {
  if (decimalOdds <= 1.0 || prob <= 0) return 0;
  const b = decimalOdds - 1.0;
  const q = 1.0 - prob;
  const f = (prob * b - q) / b;
  const halfKelly = f * 0.5;
  return Math.max(0, parseFloat((halfKelly * 100).toFixed(2)));
}
```

**操作**:
- 创建 `src/utils/quant/kelly.ts`
- 剪切函数
- 验证: `npm run dev`

---

#### Task 5: 提取特征工程 → `quant/features.ts`

**目标**: 提取 `extractExtendedFeatures()`

**操作**:
- 创建 `src/utils/quant/features.ts`
- 剪切 `extractExtendedFeatures()` 和 `ExtendedTeamFeatures` 接口（已经在 types.ts 中）
- 验证: `npm run dev`

---

#### Task 6: 提取亚盘模块 → `quant/asianHandicap.ts`

**目标**: 提取亚盘/欧赔转换函数

**涉及函数**:
- `convertAsianTo1X2()`
- `convert1X2ToAsian()`
- `syncMatchToAsianHandicap()`
- `calculateDynamicAsianHandicap()`
- `extractAsianHandicapFeatures()`

**操作**:
- 创建 `src/utils/quant/asianHandicap.ts`
- 剪切上述 5 个函数
- 验证: `npm run dev`

---

#### Task 7: 提取通道计算 → `quant/channels/`

**目标**: 将 `calculateBetsModel` 内部的欧赔通道和亚盘通道逻辑拆分为独立文件

**操作**:
- 创建 `src/utils/quant/channels/oddsChannel.ts`
- 创建 `src/utils/quant/channels/asianChannel.ts`
- 从 `calculateBetsModel` 中提取对应逻辑
- 验证: `npm run dev`

---

#### Task 8: 提取融合模块 → `quant/fusion.ts`

**目标**: 提取通道融合、综合决策、结果构建逻辑

**操作**:
- 创建 `src/utils/quant/fusion.ts`
- 从 `calculateBetsModel` 中提取融合逻辑
- 验证: `npm run dev`

---

#### Task 9: 提取贝叶斯模块 → `quant/bayesian.ts`

**目标**: 提取 `calculateBayesianLiveUpdate()` 并重命名为更准确的名称

**操作**:
- 创建 `src/utils/quant/bayesian.ts`
- 剪切函数，重命名为 `calculateLiveMatchUpdate()` 或保留原名
- 验证: `npm run dev`

---

#### Task 10: 统一入口 → `quant/index.ts`

```typescript
// quant/index.ts
export * from './types';
export * from './poisson';
export * from './elo';
export * from './kelly';
export * from './features';
export * from './asianHandicap';
export * from './channels/oddsChannel';
export * from './channels/asianChannel';
export * from './fusion';
export * from './bayesian';
```

---

#### Task 11: 更新 `quantModel.ts` 为向后兼容入口

```typescript
// quantModel.ts — 精简为纯重导出，保证零破坏性
export * from './quant';
```

**操作**:
- 将 `quantModel.ts` 替换为纯重导出
- 验证: 所有 import 不受影响，`npm run dev` 无错误

---

### 第一阶段验证清单

- [ ] `npm run dev` 启动无编译错误
- [ ] 所有 Tab 切换正常
- [ ] 球队选择 → 计算正常
- [ ] 亚盘/欧赔联动正常
- [ ] 贝叶斯实时监控正常
- [ ] AI 分析正常
- [ ] `npm run test` 全部通过
- [ ] `npm run build` 无错误

---

## 第二阶段：AppNew.tsx 进一步拆分 (P1)

### 当前状态

AppNew.tsx 已从 2427 行降至 1802 行，已提取 5 个组件，但仍有大量 JSX 可直接提取。

### 需要进一步提取的组件

#### Task 12: 提取 Dashboard 左侧面板 → `MatchSetupPanel.tsx`

**目标**: 提取 Dashboard 左侧的对决阵容选择面板（L589-L916）

**涉及内容**:
- Fixture 赛程选择
- 主客队联赛/球队选择
- 亚盘配置面板
- 重新计算按钮
- 自定义战力按钮
- 重置按钮

**Props**:
```typescript
interface MatchSetupPanelProps {
  fixtures: Fixture[];
  isFixturesLoading: boolean;
  fixtureSyncMsg: string;
  fixtureSyncSource: string;
  selectedFixtureId: string;
  onSelectFixture: (id: string) => void;
  loadRealTimeFixtures: () => void;
  odds: { home: number; draw: number; away: number };
  goalsLine: number;
  setGoalsLine: (v: number) => void;
  asianHandicap: AsianHandicapParams;
  setAsianHandicap: Dispatch<SetStateAction<AsianHandicapParams>>;
  returnRate: number;
  setReturnRate: (v: number) => void;
  convertedOdds: { homeOdds: number; drawOdds: number; awayOdds: number };
  onRecalculate: () => void;
  isStatsCustomized: boolean;
  onToggleStats: () => void;
  onReset: () => void;
}
```

---

#### Task 13: 提取自定义战力面板 → `CustomStatsPanel.tsx`

**目标**: 提取 `isStatsCustomized` 条件渲染的调试器面板（L918-L1055）

**Props**:
```typescript
interface CustomStatsPanelProps {
  homeName: string;
  awayName: string;
  customStats: CustomizedStats;
  setCustomStats: Dispatch<SetStateAction<CustomizedStats>>;
  onRecalculate: () => void;
}
```

---

#### Task 14: 提取 Dashboard 右侧结果面板 → `ResultsPanel.tsx`

**目标**: 将右侧所有结果展示区提取为独立组件（L1431-L2138）

**涉及内容**:
- 风险警示栏
- 计算错误提示
- 聚合决策中枢
- 推荐方向决策板
- 概率对比可视化
- 贝叶斯实时监测台
- 角球策略图
- 凯利 & xPts 面板
- 犯规惩罚面板
- Poisson 预期 & 大小球
- 雷达图
- AI 专家点评

**Props**:
```typescript
interface ResultsPanelProps {
  riskAlerts: RiskAlert[];
  clearRiskAlert: (id: string) => void;
  calculationError: string | null;
  onRetryCalculation: () => void;
  results: PredictionResults | null;
  convertedOdds: { homeOdds: number; drawOdds: number; awayOdds: number };
  home: TeamStats;
  away: TeamStats;
  liveMatchState: LiveMatchState;
  liveMatchDispatch: Dispatch<LiveMatchAction>;
  liveResults: LiveResults | null;
  goalsLine: number;
  aiAnalysis: string | null;
  isAiLoading: boolean;
  aiValidationWarning: string | null;
  onFetchAiAnalysis: () => void;
}
```

---

#### Task 15: 提取 Footer → `PageFooter.tsx`

**目标**: 提取页脚（L2399-L2413）

**Props**: 无（纯静态展示）

---

### 第二阶段最终目标

```
AppNew.tsx: 1802 → ~300 行
```

---

## 第三阶段：算法优化 (P2)

### Task 16: 修复 Poisson 数值稳定性

**已完成于 Task 2**（在拆分 poisson.ts 时同时修复）

---

### Task 17: 补充单元测试

**目标**: 覆盖所有拆分后的纯函数模块

```
src/__tests__/
├── quant/
│   ├── poisson.test.ts       # poisson(), dixonColesAdjustment()
│   ├── elo.test.ts           # getTeamElo(), updateElo()
│   ├── kelly.test.ts         # calculateKellyFraction()
│   ├── asianHandicap.test.ts # convertAsianTo1X2(), convert1X2ToAsian()
│   └── features.test.ts      # extractExtendedFeatures()
```

**测试示例**:

```typescript
// quant/poisson.test.ts
import { describe, it, expect } from 'vitest';
import { poisson, dixonColesAdjustment } from '../../utils/quant/poisson';

describe('poisson', () => {
  it('lambda=0 时只有 k=0 返回 1', () => {
    expect(poisson(0, 0)).toBe(1);
    expect(poisson(1, 0)).toBe(0);
  });

  it('标准泊松分布计算', () => {
    const result = poisson(2, 1.5);
    expect(result).toBeCloseTo(0.251, 2);
  });

  it('大 k 值不溢出', () => {
    expect(() => poisson(100, 90)).not.toThrow();
  });

  it('大 lambda 值不溢出', () => {
    const result = poisson(50, 50);
    expect(result).toBeGreaterThan(0);
    expect(result).toBeLessThan(1);
  });
});

describe('dixonColesAdjustment', () => {
  it('0-0 比分调整', () => {
    const result = dixonColesAdjustment(0, 0, 1.5, 1.2, -0.075);
    expect(result).toBeCloseTo(1.135, 3);
  });
});
```

---

### Task 18: 完善 TypeScript 类型

**目标**: 消除所有 `any` 类型，增强类型安全

```typescript
// 需要修复的类型
// 1. useFixtureSync 中的 fixtures: any[] → fixtures: Fixture[]
// 2. TeamContext 中的 teamsPageTeamStats: any → TeamStats | null
// 3. setTeamsPageTeam 的 stats: any → TeamStats

// 新增类型
export interface Fixture {
  id: string;
  name: string;
  homeTeamId: string;
  awayTeamId: string;
  homeLeague: string;
  awayLeague: string;
  stageCn: string;
  matchTime: string;
  defaultOdds: { home: number; draw: number; away: number };
  defaultGoalsLine: number;
  competitionType: 'League' | 'Cup';
}
```

---

## 第四阶段：性能优化 (P3)

### Task 19: 拆分 Context 减少重渲染

**问题**: `TeamContext` 包含 5 个 Tab 共用的状态，任何 Tab 切换都会触发所有订阅者重渲染

**方案**: 拆分为多个独立 Context

```typescript
// 拆分方案
src/context/
├── TeamContext.tsx          # 保持现有（球队选择相关）
├── TeamSelectionContext.tsx # 新增：仅主客队选择状态
├── TabContext.tsx           # 新增：仅 activeTab 状态
└── LiveMatchContext.tsx     # 保持现有
```

**操作**:
- 创建 `TabContext.tsx`，仅管理 `activeTab` 状态
- 创建 `TeamSelectionContext.tsx`，管理主客队选择
- `TeamContext.tsx` 内部组合这两个 Context

---

### Task 20: 添加 React.memo 包裹大组件

为以下组件添加 `React.memo`：
- `ResultsPanel`
- `MatchSetupPanel`
- `AggregationDecisionCenter`
- `TeamRadarChart`

---

## 第五阶段：工程化改进 (P4)

### Task 21: 配置 ESLint 规则

```json
// .eslintrc.json 新增规则
{
  "rules": {
    "@typescript-eslint/no-explicit-any": "warn",
    "@typescript-eslint/explicit-function-return-type": "off",
    "react-hooks/exhaustive-deps": "warn"
  }
}
```

### Task 22: 删除 App.tsx 旧文件，启用 AppNew.tsx

**操作**:
1. 将 `AppNew.tsx` 重命名为 `AppNewContent.tsx`（或直接作为 App.tsx 的替代）
2. 更新 `App.tsx` 的导入
3. 删除旧的 `App.tsx` 中的 `AppContent` 函数体

---

## 实施顺序总览

```
📅 第 1-2 天 (P0)
├── Task 1:  提取类型定义 → quant/types.ts
├── Task 2:  提取 Poisson 模块 → quant/poisson.ts (+ 修复数值稳定性)
├── Task 3:  提取 Elo 模块 → quant/elo.ts (+ 新增 updateElo)
├── Task 4:  提取凯利模块 → quant/kelly.ts
├── Task 5:  提取特征工程 → quant/features.ts
└── Task 10: 统一入口 → quant/index.ts

📅 第 3-4 天 (P0)
├── Task 6:  提取亚盘模块 → quant/asianHandicap.ts
├── Task 7:  提取通道计算 → quant/channels/
├── Task 8:  提取融合模块 → quant/fusion.ts
├── Task 9:  提取贝叶斯模块 → quant/bayesian.ts
└── Task 11: 更新 quantModel.ts 为向后兼容入口

📅 第 5-6 天 (P1)
├── Task 12: 提取 MatchSetupPanel
├── Task 13: 提取 CustomStatsPanel
├── Task 14: 提取 ResultsPanel
└── Task 15: 提取 PageFooter

📅 第 7-8 天 (P2)
├── Task 17: 补充单元测试
└── Task 18: 完善 TypeScript 类型

📅 第 9-10 天 (P3-P4)
├── Task 19: 拆分 Context
├── Task 20: 添加 React.memo
├── Task 21: ESLint 配置
└── Task 22: 清理旧文件
```

---

## 风险与应对

| 风险 | 等级 | 应对 |
|------|------|------|
| `calculateBetsModel` 拆分后计算结果不一致 | 🔴高 | 每步拆分后对比原始输出，确保一致性 |
| 循环依赖（quant 模块之间） | 🟠中 | 单向依赖：types → poisson/elo/kelly → channels → fusion |
| 组件提取后 Props 传递遗漏 | 🟠中 | 每个组件提取后立即测试对应功能 |
| 大范围重构导致合并冲突 | 🟡低 | 单文件拆分，每步提交 |

---

## 成功标准

- [ ] `quantModel.ts` 从 3222 行降至 ~30 行（纯重导出）
- [ ] `AppNew.tsx` 从 1802 行降至 ~300 行
- [ ] 核心算法单元测试覆盖率 > 80%
- [ ] 所有 `any` 类型消除
- [ ] `npm run dev` / `npm run build` / `npm run test` 全部通过
- [ ] 现有功能零回归